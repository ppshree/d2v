import React, { FC, useEffect } from 'react';
import '../Admin.css';

export const StudentList: FC = () => {
  useEffect(() => {
    //MAKE API CALLS
  }, []);

  return <h1>This is admin student-list page</h1>;
};

// eslint-disable-next-line import/no-default-export
export default StudentList;
